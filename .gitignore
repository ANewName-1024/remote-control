__pycache__/
*.pyc
*.pyo
*.egg-info/
dist/
build/
*.log
node_modules/
.DS_Store

# Runtime deploy artifacts
server/static/app/
server/uploads/
server/agent_logs/

# Developer-local launch scripts (point at a specific user profile
# under C:\Users\<user>\AppData\... so they aren't portable). The
# leading underscore follows the convention of marking a file as
# "scratch / not for the repo"; matching pattern is _*.ps1 at repo
# root.
_*.ps1

# Backup of previously corrupted AGENT.md (kept for diff reference)
AGENT.md.old

# Deploy artifacts (real config + generated tarballs)
deploy/.env.vps
deploy/.env.windows
deploy/dist/
deploy/tools/
